#include <stdio.h>
#include <string.h>

void Chatbot() {
char str[100] ;
char str1[100] = "sad" ;
char str2[100] = "help" ; 
char str3[100] = "exit" ;
char str4[100] = "happy";
    printf("Entrez une chat : ");
        scanf("%s", &str);
        for (int i = 0 ; str[i] != '\0'; i++) {

        }
        char result = strstr(str4 , happy) 
            printf("Hi! Great to hear that!");
        }else if (result = strstr(str1 , sad)) {
            printf("Oh no! Hope things get better soon");
        }else if (result = strstr(str3 , exit)) {
            printf("Goodbye!");
        }else (result = strstr(str2 , help)) {
            printf("Sure! What do you want to talk about?");
        }

}

int main() {
Chatbot();
return 0;
}
